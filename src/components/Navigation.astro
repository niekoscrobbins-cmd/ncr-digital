---
import { navLinks, contactHref, contactCtaLabel } from '@/data/nav';
---

<header>
  <nav
    class="fixed top-0 left-0 right-0 z-50 border-b border-ncr-border bg-ncr-bg/90 backdrop-blur-md"
    aria-label="Primary navigation"
  >
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">

        <!-- Logo -->
        <a href="/" class="font-display text-xl group" aria-label="NCR Digital home">
          <span class="text-ncr-text group-hover:text-ncr-accent transition-colors duration-200">NCR</span><!--
          --><span class="text-ncr-accent">D</span><!--
          --><span class="text-ncr-text group-hover:text-ncr-accent transition-colors duration-200">igital</span>
        </a>

        <!-- Desktop links -->
        <div class="hidden md:flex items-center gap-8">
          {navLinks.map(({ label, href }) => (
            <a
              href={href}
              class="text-ncr-muted hover:text-ncr-text text-sm font-medium transition-colors duration-200 relative group"
            >
              {label}
              <span class="absolute -bottom-0.5 left-0 h-px w-0 bg-ncr-accent transition-all duration-300 group-hover:w-full" />
            </a>
          ))}
          <a
            href={contactHref}
            class="px-5 py-2 rounded-pill bg-ncr-accent text-white text-sm font-medium hover:bg-ncr-accentHover transition-all duration-200"
          >
            {contactCtaLabel}
          </a>
        </div>

        <!-- Mobile hamburger -->
        <button
          id="mobile-menu-btn"
          class="md:hidden p-2 text-ncr-muted hover:text-ncr-text transition-colors"
          aria-label="Open navigation menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
        >
          <svg width="22" height="22" viewBox="0 0 22 22" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="square">
            <line x1="2" y1="5"  x2="20" y2="5"  />
            <line x1="2" y1="11" x2="20" y2="11" />
            <line x1="2" y1="17" x2="20" y2="17" />
          </svg>
        </button>
      </div>

      <!-- Mobile menu (hidden by default) -->
      <div
        id="mobile-menu"
        class="hidden md:hidden pb-5 space-y-1 border-t border-ncr-border pt-4"
        role="menu"
      >
        {navLinks.map(({ label, href }) => (
          <a
            href={href}
            class="block text-ncr-muted hover:text-ncr-text text-sm py-2.5 px-1 transition-colors"
            role="menuitem"
          >
            {label}
          </a>
        ))}
        <div class="pt-3">
          <a
            href={contactHref}
            class="inline-flex items-center px-5 py-2.5 rounded-pill bg-ncr-accent text-white text-sm font-medium hover:bg-ncr-accentHover transition-all duration-200"
          >
            {contactCtaLabel}
          </a>
        </div>
      </div>
    </div>
  </nav>
</header>

<script>
  const btn  = document.getElementById('mobile-menu-btn')!;
  const menu = document.getElementById('mobile-menu')!;

  btn.addEventListener('click', () => {
    const isOpen = !menu.classList.contains('hidden');
    menu.classList.toggle('hidden');
    btn.setAttribute('aria-expanded', String(!isOpen));
  });

  // Close menu when a link is clicked
  menu.querySelectorAll('a').forEach(link =>
    link.addEventListener('click', () => {
      menu.classList.add('hidden');
      btn.setAttribute('aria-expanded', 'false');
    })
  );
</script>
