---
export interface Props {
  eyebrow?: string;
  title: string;
  subtitle?: string;
  ctaLabel?: string;
  ctaHref?: string;
  secondaryCtaLabel?: string;
  secondaryCtaHref?: string;
  align?: 'left' | 'center';
  size?: 'xl' | 'lg';
}

const {
  eyebrow,
  title,
  subtitle,
  ctaLabel,
  ctaHref,
  secondaryCtaLabel,
  secondaryCtaHref,
  align = 'left',
  size = 'xl',
} = Astro.props;

const alignClass = align === 'center' ? 'items-center text-center mx-auto' : 'items-start text-left';
const titleClass = size === 'xl' ? 'text-4xl sm:text-display-lg lg:text-display-xl' : 'text-4xl sm:text-display-md lg:text-display-lg';
---

<section class="relative pt-32 pb-16 sm:pt-40 sm:pb-24 bg-ncr-bg overflow-hidden">
  <slot name="background" />
  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class={`flex flex-col gap-6 max-w-4xl ${alignClass}`}>
      {eyebrow && (
        <p class="text-xs tracking-wide uppercase text-ncr-muted">
          {eyebrow}
        </p>
      )}
      <h1 class={`${titleClass} font-display text-ncr-text`}>
        {title}
      </h1>
      {subtitle && (
        <p class="text-lg sm:text-xl text-ncr-muted leading-relaxed max-w-2xl">
          {subtitle}
        </p>
      )}
      {(ctaLabel || secondaryCtaLabel) && (
        <div class="flex flex-wrap items-center gap-6 mt-2">
          {ctaLabel && ctaHref && (
            <a
              href={ctaHref}
              class="px-6 py-3 rounded-pill bg-ncr-accent text-white text-sm font-medium hover:bg-ncr-accentHover active:scale-[0.98] transition-all duration-200"
            >
              {ctaLabel}
            </a>
          )}
          {secondaryCtaLabel && secondaryCtaHref && (
            <a
              href={secondaryCtaHref}
              class="text-sm font-medium text-ncr-text underline decoration-ncr-rule underline-offset-4 hover:decoration-ncr-accent hover:text-ncr-accent transition-colors duration-200"
            >
              {secondaryCtaLabel}
            </a>
          )}
        </div>
      )}
    </div>
  </div>
</section>
